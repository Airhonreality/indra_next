/**
 * INDRA ADAPTER REGISTRY INITIALIZATION
 * Import this file once at the root of your application or in API routes
 * to ensure all storage providers are registered in the global registry.
 */

import './google-drive';
import './google-sheets';
import './notion';
import './storage';
